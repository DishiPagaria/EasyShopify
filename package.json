{
  "name": "ecommerce",
  "version": "1.0.0",
  "scripts": {
    "frontend:build": "cd frontend && npm install && npm run build",
    "start": "node backend/index.js"
  },
  "dependencies": {
    "date-fns": "^3.6.0"
  }
}
